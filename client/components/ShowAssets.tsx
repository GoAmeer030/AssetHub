'use client';

import { assetType } from '@/types/assetType';

export default function ShowAssets({ assets }: { assets: assetType[] }) {
  return (
    <div>
      <p>this is a table</p>
    </div>
  );
}
