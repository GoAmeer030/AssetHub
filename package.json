{
  "devDependencies": {
    "@commitlint/cli": "^18.6.1",
    "@commitlint/config-conventional": "^18.6.1",
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5"
  },
  "scripts": {
    "install-all": "concurrently \"npm i\" \"cd client && npm i\" \"cd server && npm i && npx prisma generate\"",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run start --prefix server\"",
    "format": "prettier --write",
    "prepare": "husky",
    "lint:fix": "eslint --fix ."
  }
}
